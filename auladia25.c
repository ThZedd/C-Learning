// o cpu faz control e aritmetica, tem que entrar informação dentro do cpu para o computador funcionar 
// o C foi criado por Dennis Ritchie em 1972 na Bell Labs
// C é uma linguagem compilada, i.e, o código fonte é traduzido para código de máquina, e depois executado.
// Na realidade, todas as linguagens de programação são compiladas, mas nem todas são compiladas para código máquina
#include <stdio.h>

int main() {
int number = 17;
float proj = 0;

proj = (16 *0.9)*0.5;
printf("%f", proj);
return 0;
}